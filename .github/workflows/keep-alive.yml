name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/5 * * * *" # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render API
        run: |
          curl -s https://aviation-shwu.onrender.com/aviation-app/api/v1/health
